import React from "react";
import styled from "styled-components";

const Schedule = () => {
  return <Wrapper>예약가능 목록</Wrapper>;
};
export default Schedule;

const Wrapper = styled.div`
  background-color: lightblue;
`;
